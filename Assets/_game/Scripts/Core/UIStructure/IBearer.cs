using System;
using System.Collections;
using System.Collections.Generic;
using Core.UiStructure;
using UnityEngine;
using UnityEngine.UI;

namespace Core.UIStructure
{
    /*public interface IBearer
    {
        Transform transform { get; }
        Canvas Canvas { get; }
        CanvasScaler Scaler { get; }
        Coroutine StartCoroutine(IEnumerator enumerator);
        List<IService> GetServices();
        bool GetBlock<T>(out T block) where T : IService;
        bool GetBlock<T>(out T block, Type type) where T : IService;
        T Create<T>(T prefab, BlockSequenceSettings settings = null) where T : MonoBehaviour, IService;
        void Insert<T>(T instance) where T : MonoBehaviour, IService;
        void Remove(IService block);
        void Refresh();
    }*/
}
